[project]
name = "blank-app-template"
version = "0.1.0"
description = "A simple Streamlit app template."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "qrcode[pil]>=8.2",
    "streamlit>=1.61.1",
]

[tool.uv]
package = false
